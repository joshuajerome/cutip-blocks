# Patch Notes

## v0.1.0 (2026-03-24)

### Features

- **cap100** — Initial block library: 34 blocks across 9 categories (container, ssh, file, k8s, crictl, download, config, validate, service)
- `@block` decorator with `BlockMeta` (name, category, action)
- `BlockRegistry.discover()` auto-finds all blocks via `pkgutil`
- `SSHSession` with credential redaction and persistent connection reuse
- Structured logging with `[BlockName]` prefix on all block executions
# v0.1.1
# v0.1.1 retry
# v0.2.0
